# ThinkBase

## Setup
- Update `Rakefile` to have the correct theme
- Run `rake install` to install dependencies

## Compiling assets
This project uses Browsersync, Sass and RequireJS
- Run `rake serve`
